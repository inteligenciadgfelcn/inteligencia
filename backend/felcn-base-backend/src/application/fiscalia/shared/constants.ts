/**
 * Constantes del módulo Fiscalía (recepción MP → POL)
 * Los endpoints son abiertos: la seguridad la aplica el hub de
 * interoperabilidad que publica estas APIs.
 */

/** Schema de staging en la base de datos felcn_siii */
export const SCHEMA_FISCALIA = 'fiscalia'

/** Prefijo de rutas del módulo (bajo el prefijo global PATH_SUBDOMAIN) */
export const RUTA_FISCALIA = 'v1/external/fiscalia'
